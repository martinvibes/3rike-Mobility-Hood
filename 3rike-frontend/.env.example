# Base URL of the 3rike backend API. Used by src/lib/api.ts.
#
# Local dev:    http://localhost:8080  (run 3rike-backend with `docker compose up`)
# Deployed:     https://threerike-mobility.onrender.com  (Render free tier — first
#               request after inactivity may take ~60s for cold start)
VITE_API_URL=https://threerike-mobility.onrender.com
